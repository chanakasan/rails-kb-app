<%= form_for(@knowledgebase) do |f| %>
  <% if @knowledgebase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@knowledgebase.errors.count, "error") %> prohibited this knowledgebase from being saved:</h2>

      <ul>
      <% @knowledgebase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
